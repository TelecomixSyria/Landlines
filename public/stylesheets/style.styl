a
 	color: #00B7FF

#map-canvas
	position: absolute
	margin: 0px
	height: 100%
	width: 100%
	top: 42px

#map-canvas label {
	width: auto; 
	display:inline; 
}

#map_canvas label {
	width: auto; 
	display:inline; 
}

#geocomplete { 
	width: 200px;
}

.map_canvas { 
  width: 800px; 
  height: 400px; 
  margin: 10px 20px 10px 0;
}

#map_canvas img {
	max-width: none;
}

#map-canvas img {
	max-width: none;
}

.navbar
	position: absolute
	right: 0px
	left: 0px
	z-index: 500
	visibility: visible